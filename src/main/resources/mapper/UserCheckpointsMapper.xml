<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluemsun.mapper.UserCheckpointsMapper">

    <select id="getCheckpointsUnLockByUserId" resultType="java.lang.Integer">
        select checkpoints_id from tb_user_checkpoints where user_id = #{userId};
    </select>


</mapper>